devices:
  - name: "cp_member_1"
    role: "cp-cluster-member"
    ip_address: "10.200.0.2"

  - name: "aruba_vsx_node"
    role: "aruba-vsx-node"
    ip_address: "10.200.0.3"

interval: 60
webhook_url: #"" # Optional, replace with your actual webhook URL

# Example of an asymmetric routing burst check configuration
asymmetric_routing_burst_interval: 300
max_duration_seconds: 10
